interface:
  display_name: "Самопроверка результата"
  short_description: "Проверка готовности без лишних повторов"
  default_prompt: "Примени $self-check: проверь готовность результата, используй уже выполненные проверки и исправь существенные ошибки; если нужна числовая оценка, примени $universal-evaluator."
policy:
  allow_implicit_invocation: true
